# 1.12.0

- Characters typed with Option (€, {, [, |) are no longer eaten when a program uses the kitty keyboard protocol, e.g. Claude Code
- Bookmark colors are configurable, and a new reload-config IPC command reloads the config without restarting
- Reopening a bookmark or a closed session from search runs its resume line
